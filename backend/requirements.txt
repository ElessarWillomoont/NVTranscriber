# Core API
fastapi>=0.115.0
uvicorn[standard]>=0.30.0
pydantic>=2.0.0

# Database
sqlalchemy>=2.0.0
aiosqlite>=0.20.0

# File upload support
python-multipart>=0.0.12

# GPU monitoring
pynvml>=11.5.0

# ONNX runtime (GPU-accelerated Silero VAD)
onnxruntime-gpu>=1.20.0

# Testing
pytest>=7.4.0
httpx>=0.25.0

# Audio Processing
soundfile>=0.12.1

# ------------------------------------------------------------
# PyTorch must be installed SEPARATELY with the CUDA wheel.
# Run setup.ps1, or manually:
#
#   pip install torch torchaudio --index-url https://download.pytorch.org/whl/cu124
#
# Replace cu124 with your installed CUDA version (cu118, cu121, …).
# ------------------------------------------------------------
